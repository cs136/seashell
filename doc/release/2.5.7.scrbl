#lang scribble/manual
@(require "../utils.rkt")

@seashell-title[#:tag "build-2.5.7"]{v.2.5.7}
Summary:
@itemlist[
  @item{Fixed Marmoset assignmet selection: @issue[560].}
  @item{Added support for persistent undo history across sessions: @pr[547] @pr[551] @pr[552].}
  @item{Allowed runner file to be stored in common/.}
]
