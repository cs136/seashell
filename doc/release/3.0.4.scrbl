#lang scribble/manual
@(require "../utils.rkt")

@seashell-title[#:tag "build-3.0.4"]{v.3.0.4}
Summary:
@itemlist[
  @item{Disable offline mode.}
  @item{Fix issue with where memory leaks weren't always being reported.}
]
