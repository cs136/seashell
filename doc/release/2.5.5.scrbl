#lang scribble/manual
@(require "../utils.rkt")

@seashell-title[#:tag "build-2.5.5"]{v.2.5.5}
Summary:
@itemlist[
  @item{Warn when line is greater than 80 characters.}
  @item{Recusively resolve dependencies.}
  @item{Ungroup files in file list.}
  @item{Reset files to original skeleton file upon renamed/delete.}
  @item{Fix off-by-one when underlining errors.}
  @item{Add new return code for assertion failure.}
  @item{Guess Marmoset project name correctly in submission.}
]
